/**
 * Role Constants
 * AURA ARCHIVE - User roles
 */

const ROLES = {
    ADMIN: 'ADMIN',
    CUSTOMER: 'CUSTOMER',
};

const ROLE_LIST = Object.values(ROLES);

module.exports = {
    ROLES,
    ROLE_LIST,
};
